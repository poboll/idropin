<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.idropin.infrastructure.persistence.mapper.FileChunkMapper">

    <select id="findByUploadId" resultType="com.idropin.domain.entity.FileChunk">
        SELECT id, upload_id, file_id, file_name, total_size, file_md5, chunk_number, chunk_size, storage_path, uploader_id, status, updated_at
        FROM file_chunk WHERE upload_id = #{uploadId} ORDER BY chunk_number
    </select>

    <select id="findByUploadIdAndChunkNumber" resultType="com.idropin.domain.entity.FileChunk">
        SELECT id, upload_id, chunk_number, status, storage_path
        FROM file_chunk WHERE upload_id = #{uploadId} AND chunk_number = #{chunkNumber} LIMIT 1
    </select>

    <delete id="deleteByUploadId">
        DELETE FROM file_chunk WHERE upload_id = #{uploadId}
    </delete>

</mapper>
